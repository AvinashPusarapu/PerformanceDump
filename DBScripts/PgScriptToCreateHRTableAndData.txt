CREATE TABLE hr_data (
	user_id varchar(50) NULL,
	fullname varchar(50) NULL,
	firstname varchar(50) NULL,
	middlename varchar(50) NULL,
	lastname varchar(50) NULL,
	identitytype varchar(50) NULL,
	userstatus varchar(50) NULL,
	accounttype varchar(50) NULL,
	departmentname varchar(50) NULL,
	region varchar(50) NULL,
	paystatus varchar(50) NULL,
	issuspended varchar(50) NULL,
	badgetype varchar(50) NULL,
	"Floor" varchar(50) NULL,
	jobtitle varchar(50) NULL,
	actionreason varchar(50) NULL,
	reason varchar(250) NULL,
	physicalaccess varchar(250) NULL,
	thresholdflag varchar(50) NULL,
	bussinessunitandworklocation varchar(250) NULL,
	bussinessunitdescription varchar(250) NULL,
	managername varchar(100) NULL,
	companies varchar(250) NULL,
	badgeid int8 NULL,
	personalid int8 NULL,
	businessunit varchar(50) NULL,
	worklocation varchar(50) NULL,
	validto date NULL,
	subtype varchar(50) NULL,
	validfrom date NULL,
	description varchar(250) NULL,
	technologies varchar(250) NULL,
	"type" varchar(50) NULL,
	displayname varchar(255) NULL,
	email varchar(255) NULL,
	intstatus _int4 NULL,
	country varchar(255) NULL,
	created_date timestamp NULL,
	managerid varchar(255) NULL
);

WITH RECURSIVE row_generator AS (
    SELECT 1 AS n
    UNION ALL
    SELECT n + 1
    FROM row_generator
    WHERE n < 10001 -- Change this number to the desired number of rows
)
INSERT INTO aehsc.hr_data (
    user_id,
    fullname,
    firstname,
    middlename,
    lastname,
    identitytype,
    userstatus,
    accounttype,
    departmentname,
    region,
    paystatus,
    issuspended,
    badgetype,
    "Floor",
    jobtitle,
    actionreason,
    reason,
    physicalaccess,
    thresholdflag,
    bussinessunitandworklocation,
    bussinessunitdescription,
    managername,
    companies,
    badgeid,
    personalid,
    businessunit,
    worklocation,
    validto,
    subtype,
    validfrom,
    description,
    technologies,
    "type",
    displayname,
    email,
    country,
    created_date,
    managerid
)
SELECT
    md5(random()::text),
    md5(random()::text),
    md5(random()::text),
    md5(random()::text),
    md5(random()::text),
    'Passport',
    'Active',
    'Regular',
    'HR Department',
    'North',
    'Paid',
    'No',
    'Standard',
    '1st Floor',
    'HR Manager',
    'Hiring',
    'New hire',
    'Access granted',
    'Yes',
    'HR - North Office',
    'HR Department, North Office',
    md5(random()::text),
    md5(random()::text),
    random() * 100000,
    random() * 100000,
    'HR',
    'North Office',
    CURRENT_DATE + (random() * 365)::int,
    'Full-time',
    CURRENT_DATE - (random() * 365)::int,
    'HR Data Entry',
    'Java, SQL',
    'Employee',
    md5(random()::text),
    md5(random()::text) || '@example.com',
    
    md5(random()::text),
    now(),
    md5(random()::text)
FROM row_generator;